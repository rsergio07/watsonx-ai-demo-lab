// Assisted by watsonx Code Assistant 
// Code generated by WCA@IBM in this programming language is not approved for use in IBM product development.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: "500m"
            memory: "1Gi"
            livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5

// Assisted by watsonx Code Assistant 
# nginx-deployment.yaml

apiVersion: apps/v1 # Kubernetes API version for Deployments
kind: Deployment # Type of resource being defined
metadata:
  name: nginx-deployment # Name of the Deployment resource
spec:
  replicas: 3 # Number of replicas to run
  selector:
    matchLabels:
      app: nginx # Selector to match pods for this Deployment
  template:
    metadata:
      labels:
        app: nginx # Labels to apply to pods
    spec:
      containers:
      - name: nginx # Name of the container
        image: nginx:latest # Image to use for the container
        ports:
        - containerPort: 80 # Port to expose on the container
        resources:
          limits:
            cpu: "500m" # CPU limit for the container
            memory: "1Gi" # Memory limit for the container
            livenessProbe: # Liveness probe configuration
              httpGet:
                path: / # Path to probe
                port: 80 # Port to probe
              initialDelaySeconds: 5 # Initial delay before probing
              periodSeconds: 5 # Period between probes
            readinessProbe: # Readiness probe configuration
              httpGet:
                path: / # Path to probe
                port: 80 # Port to probe
              initialDelaySeconds: 5 # Initial delay before probing
              periodSeconds: 5 # Period between probes


