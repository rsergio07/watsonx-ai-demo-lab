// Assisted by watsonx Code Assistant 
// Code generated by WCA@IBM in this programming language is not approved for use in IBM product development.
// This code is a Kubernetes service definition file for an Nginx server. It defines a service named "nginx-service" 
// that selects pods with the label "app nginx" and exposes them on port 80. The service type is set to "LoadBalancer" 
// and the load balancer IP is set to 192.168.1.100.
//nginx-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
  loadBalancerIP: 192.168.1.100
